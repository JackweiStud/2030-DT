# case1 正式运行资源

从已通过人工验收的 web-static/case1/assets 复制被正式页面实际使用的图片；不运行时引用静态目录。未复制假 Shell、3D 截图或 _qa。RF 底图仍含参考色块，待用户提供无网格图后替换本目录 rf-map.png 并完成标定。正式 GLB 位于本目录 `3D/`（`Beijing_Geometry.glb` / `Beijing_Material.glb`），由适配服务只读流式提供；体积较大，入库便于换机演示。
