# case3-v2 正式运行资源

本目录是 Case3 V2 正式运行资源；由 `03-design/case3V1/assets/` 复制而来，再同步到 `code/web/assets/case3-v2/`。
React 只允许 import `code/web/assets/case3-v2/`。

未复制 Shell 已拥有的 `nav-bg.png` / `cloud-site.png` / `huawei-logo.png` / `menu-bars.png`，以及现场环境 `video-*`（弹窗仍归 Shell）。
未复制已被整图取代的 `ue-bubble-pin.png` / `ue-ground-dot.png`，以及 CSS 未引用的平面/箭头切图。

`icon-rotate-ccw.svg` 来自既有 `04-runtime-assets/case3/`，供地图手动复位。
