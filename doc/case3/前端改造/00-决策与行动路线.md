# 决策与行动路线

- 日期：2026-08-19
- 状态：P0 ✅，P1 ✅，P2 ✅。P3 Gate 1.5 静态页已生成并完成本地检查，待用户人工 YES。

## 1. 已锁定决策

| # | 决策 | 说明 |
|---|---|---|
| D1 | case3 换皮 + 全站新导航 | case2 **业务页**不改；Node / 打桩 / 真实后端协议不改。共享 Shell **顶栏**按新 UX 换（所有 Case 同一导航） |
| D2 | 走方案 B | 新 Pencil → Gate 1.5 静态 HTML → React。不跳过静态页，不拿 PNG 直接改正式 Web |
| D3 | 保业务脑、换视觉皮 | api / reducer / controller / metrics 复用；Page、组件、CSS、切图重做 |
| D4 | 开发期加 Tab | Tab id 仍是导航位 `case5`；**文案 `DT for Comm new`**。旧 `DT for Comm` 保留旧皮。接口仍是 case3 |
| D5 | `case5` 不是新业务 Case | 仍打 `/api/case3/*`；控制文件 `case="case3"`；打桩 `start:case3`；截图 `out/case3/`。禁止 `/api/case5`、`case:"case5"` |
| D6 | 忙锁共用 | case3 与 case5 任一侧 Start/ReInit 等待，锁定全部其他 Tab。切 Tab 会 `POST init`，不要中途对照 |
| D7 | 收工合回 | 新皮稳定后迁入 case3 Tab，删除 case5。成品仍是四个 Tab，正式入口仍是 `DT for Comm` |
| D8 | 旧设计源不覆盖 | `03-design/case3/case3-dt-com.pen` 与 `web-static/case3/` 只归档。新文件用 v2 名 |
| D9 | UX PNG 不是视觉契约 | 必须先 Pencil 冻结，再做静态页，再进 React |
| D10 | 不把讨论写进 `state.md` | 等 Gate 1 v2 冻结后再更新项目状态 |
| D11 | 暂停键 | 只当忙态图标，不发后端 pause/cancel |
| D12 | 2D / 3D | 2D 选中；3D 可见但禁用（灰），不隐藏；本期只接 2D |
| D13 | 点位窗口 | 切图 P1–P20 是窗口；动态 N，可见最近 20 |
| D14 | 点位波束回溯 | 只显示 BeamID；切图 X/Y 作废；列头不可点回看 |
| D15 | 全站新导航 | 改共享 Shell。开发期 **五个 Tab**（多一个 `DT for Comm new`）。三杠装饰不可点 |
| D16 | 单地图开新轮清图 | 路线/UE/实时点位共享一份。点下一侧 Start 时地图先回初始再跑；底栏历史保留 |
| D17 | 本轮 Pencil 五态 | 初始、无DT运行、无DT完成、有DT运行、有DT完成。重置/失败不进本轮 |

## 2. 分层（必须守住）

```text
新 UX PNG/切图 ──► Pencil v2 ──► web-static/case3-v2 ──► React 新皮（先挂 case5）
                                      │
真实后端/打桩 ──文件──► Node /api/case3/* ──► 业务脑（不换）──► 旧皮 case3 Tab
                                                      └──► 新皮 case5 Tab
```

皮可以换绑定位置，不能换数据权威。Cost 仍读 `costPct`，BA 仍同 `no` 比 `selectedBeamId`。点位波束回溯只展示 BeamID，不展示 X/Y。

## 2.1 导航栏：改 vs 不改

Shell 只有一份（`Shell.tsx` + `shell.css`，顶栏高目前 78px）。做不到「只给 case3 换导航、case2 保持旧顶栏」，除非拆掉 Shell 归属。

| | 改共享 Shell（选定） | 不改 |
|---|---|---|
| 是否符合「所有 Case 同一新导航」 | 符合 | 不符合；新 case3 身体配旧顶栏 |
| 改哪些文件 | `Shell.tsx`、`shell.css`、tokens、`assets/shell/`；Gate 1.5 共享顶栏一并换 | 零 |
| case2 业务页 | 不动 | 不动 |
| case2 观感 | **顶栏会变**；若栏高不再是 78px，内容区高度跟着变，要做一次 case2 视觉回归 | 顶栏完全不动 |
| 旧 case3 Tab | 开发期业务区仍是旧皮，但顶栏会先变成新的 | 整页都旧 |
| 四 Tab / 忙锁 / 现场环境 | 开发期五个 Tab（多 `DT for Comm new`）；P5 合回仍四 Tab。忙锁与现场环境行为不变 | 不变 |
| 三杠 | 装饰，不可点，不发明菜单 | 无 |
| 工期 | 小：一套顶栏资产 + 回归 case2 一眼 | 省工，但和最新要求冲突 |

**选定改。** Pencil 要把新顶栏画进共享 chrome，不要画进 case3 业务帧里当私有件。实现时单独改 Shell，和 case5 新皮可以同一阶段，但提交上尽量分开，方便看 case2 顶栏有没有撑坏。

## 3. 行动路线

```text
P0  新 UX 源入库
      02-ux/case3-V1/     ✅ 2026-08-19
        │
P1  填完对照表（02）     ✅ 2026-08-19
      拍板：暂停=忙态图标；2D 选中/3D 灰禁；回溯只显示 BeamID；
            全站新导航 + Tab「DT for Comm new」；P1–P20 是窗口；
            开新轮清地图；本轮 Pencil 五态
        │
P2  Gate 1 新 Pencil     ✅ 用户已确认视觉冻结 YES
      03-design/case3V1/case3V1-dt-com.pen
      Design_Analysis-v2 / Pen_Plan-v2 / Visual_Diff-v2 / Frontend_Spec-v2
      不覆盖旧 `03-design/case3/case3-dt-com.pen`
        │
P3  Gate 1.5 静态 HTML   已生成，待用户人工 YES 后接受
      新建 web-static/case3-v2/
      不接 API、不读共享目录、不实现正式状态机、不启动 Node
      本轮五态：初始、无DT运行、无DT完成、有DT运行、有DT完成
      此时还不必加 case5 Tab；静态页顶栏仍要画出五个 Tab 的新导航视觉
        │
P4  React 新皮挂到 case5
      加 Tab id=case5，文案「DT for Comm new」
      同时改共享 Shell 顶栏视觉（全站新导航）；与 case5 新皮分开提交
      复用 case3 的 api/reducer/controller/metrics
      新目录只放新皮（Page / 组件 / CSS / 资产）
      旧 case3 Tab 不准先拆
      case5 与 case3 共用 busy 锁
      逻辑单测保持绿；case5 补自己的前端隔离 E2E
        │
P5  收工
      新皮合进 case3 Tab
      删除 case5 Tab 与临时目录
      更新 e2e 选择器；case2 回归不得红
      再更新 state.md / GATE 文档
```

Gate 1.5 是假交互：右下角切视觉态，页内启动/重置不发命令。  
正式状态机只在 P4 接到 case5，P5 再回到 case3。

## 4. 各阶段产出与验收

| 阶段 | 产出 | 验收 | 现在 |
|---|---|---|---|
| P0 | `02-ux/case3-V1/` 高保真切图 | 能打开整页/切图 | ✅ |
| P1 | 02 对照表填完 | 用户确认保留/换皮/删除/新增，以及拍板项 | ✅ |
| P2 | `03-design/case3V1/case3V1-dt-com.pen` + 设计交接 md | 用户视觉 YES | ✅ 视觉冻结 YES |
| P3 | `web-static/case3-v2/` | 用户人工检查静态页 | 待人工 YES |
| P4 | Shell + `src/cases/case3-v2`（或同等新皮目录） | 打桩可跑；旧 case3 Tab 仍可用 | 未开始 |
| P5 | 四 Tab 成品，新皮在 `DT for Comm` | 逻辑单测 + E2E + 人工 8 态 | 未开始 |

## 5. 建议路径名

| 用途 | 路径 |
|---|---|
| 新 UX 输入 | `02-ux/case3-V1/`（权威）；`02-ux/case3V2/` 为解压副本，勿当第二份源 |
| 新 Pencil | `03-design/case3V1/case3V1-dt-com.pen` |
| 新静态页 | `web-static/case3-v2/` |
| 新皮 React | P4 再建，例如 `code/web/src/cases/case3-v2/`；业务脑继续 import `cases/case3` 的 api/state/hooks |
| 新运行切图 | 先入 `04-runtime-assets/case3-v2/`，P5 再替换或合并进 `04-runtime-assets/case3/` |
| 旧档案 | `02-ux/case3/`、`case3-dt-com.pen`、`web-static/case3/`、现有 `src/cases/case3` 组件 |

新皮 CSS 用独立根 class（如 `.case3v2-page`），避免和旧 `.case3-page` 同包冲突。P5 再统一。

## 6. 明确不做

- 不重做 Node、打桩、真实后端文件协议。
- 不把这次改造成大一统 Case 框架。
- 不让新皮 CSS 泄漏到 case2 / 旧 case3。
- 不把 UX 样例数字写死进正式 Web。
- 不在 YES 前覆盖旧 `.pen`、旧静态页、旧 case3 Tab。
- 不两个浏览器同时用 case3 + case5 打同一份 `DT_SHARED_DIR`。

## 7. 下一步

P3 生成 `web-static/case3-v2/` 静态页并做本地六态截图检查；通过后进入用户人工验收。旧 `03-design/case3/case3-dt-com.pen` 未改。
