# 转发给 Cursor Grok 4.5：case1 Pencil 设计

你负责 `/Users/jackwl/Code/2030-DT` 的 case1 Gate 1 Pencil 设计重建。目标是可编辑、可评审、可交给前端的设计源；本轮不写 React/Node/静态 HTML，不安装 Three.js，不实现业务。用户已授权你完成设计，常规布局选择自主推进，无需重新做需求问答。Codex 随后独立校验，用户最终视觉冻结。

## 先读

1. 仓库 `AGENTS.md`、`state.md`。
2. `doc/case1/PHASE0-SCOPE.md`、`UX-STATE-MAP.md`、`Design_Analysis.md`、`Pen_Plan.md`。
3. `/Users/jackwl/.claude/skills/ai-ux-to-pencil-reconstruction/SKILL.md` 及其结构/资产参考。
4. 当前 Shell：`code/web/src/shell/Shell.tsx`、`shell.css`，只读；UX 顶栏是旧版，不覆盖当前 Shell。

## 输出与协作边界

- 输出目标：`03-design/case1/case1-dt-construction.pen`，只通过 Pencil MCP 创建/修改。先检查是否已存在；若已有 case1 设计，读取现状保留用户改动，不盲目覆盖。
- 你只写 `03-design/case1/`：设计源、必要的设计资产、`Visual_Diff.md`、`Frontend_Spec.md`、`REVIEW-HANDOFF.md` 及截图证据。已有分析/计划在 `doc/case1/`，直接引用，不复制另一份再维护。
- Codex 同时负责 `doc/case1/` 和 `state.md`，你不要编辑这些文件；改进建议写到自己的交接报告。
- 不修改任何运行代码、`.env`、参考输入或其他 case 设计；不自动提交、暂存、推送或清理用户已有改动。开始/结束分别报告自己的改动路径。
- 如果 Pencil MCP 不可用，明确报告阻塞，不能手写 JSON 冒充 `.pen`。

## 四张视觉输入

根目录：`/Users/jackwl/Code/2030-DT/01-参考资料/case1/ux/`。

- 首页：`00首页/首页.png`
- 几何层：`01几何层/首页-环境几何层.png`
- 电磁材质层：`02电磁层/首页-电磁层.png`
- RF 层：`03RF层/首页-RF层 (1).png`

四图均为 3840×2160，设计画布按项目基准 1920×1080。必须亲自查看图和切图，保留现有灰色视觉语言、配图、信息层级和区域比例；不凭通用 dashboard 风格另行设计。复用提供的真实资产，不用整张 UX 作为页面背景冒充可编辑结构。

## 必须遵守的交互与状态

1. 四个主 frame：`case1-home`、`case1-geometry`、`case1-material`、`case1-rf`。左侧位置稳定，点击只切换右侧内容及左侧明暗，不弹新窗口。
2. 首页左侧五张菱形图全部正常显示。详情当前层图片原色，其余四张变暗。前三层仍可互相切换；再次点击当前层回首页并恢复全部正常显示。知识图谱和数字孪生应用层始终不可点击。
3. 首页先显示，两个预置 GLB 后台依次准备。两详情模型支持旋转、缩放、平移；设计只放代表图，不需要在 Pencil 跑 3D。正式实现用 Three.js。
4. 两层视角独立保存到 `code/web/.env`，跨 case/重开 Web 保留；Reset 恢复各自初始设置并持久保存。你只设计模型工具区与简短保存反馈，不实现文件操作；Reset 不等于业务 ReInit。
5. 无 Start/ReInit、无后端实时测试、无业务进度、无 RF on/off 开关。RF off/on 是同屏静态数据对比。材质图例仅说明，不增加筛选/选材功能。
6. 局部样板须覆盖模型准备中/失败、数据读取中/失败、视角保存中/成功/失败；失败提供简短局部重试。做组件变体即可，不扩展几十张全页。加载和错误不阻止左侧导航。

## 图表与视觉修正

保留设计结构但替换旧图示意数字；柱高、标签、轴域必须一致：

| 指标 | RF off | RF on | 相对变化 |
|---|---:|---:|---|
| 几何保真度 | 0.85 | 0.90 | ↑5.9% |
| 重构覆盖率 | 0.82 | 0.91 | ↑11.0% |
| 电磁信道保真度 | 0.88 | 0.92 | ↑4.5% |
| RSS 误差 | 1.20 | 0.80 | ↓33.3% |

- 数值、标签、单位、柱形和变化徽标分别可编辑。前三项轴域 0～1；RSS 误差轴上界要容纳 1.20，不套 0～1。标注这些是提供的离线样例，不是现场测试结果。
- 左侧 Layer 3 的重复“电磁层”改成“RF 层”。顶部以当前 Shell 为准。
- RF 热力机制参考 case2：低值蓝→青→黄→高值红、双线性插值、马赛克透明叠加；图例必须一致，不能直接沿用旧红橙黄绿色标。
- RF `03RF层/元素/地图.png` 已内嵌旧热力网格，用户会后补纯底图。允许暂作代表图完成布局，但在设计注释和差异报告中明确其旧色块不是 TXT 渲染，暂不验收最终热力颜色/投影。不伪造已用纯底图完成融合，不自行生成替代场地图。

## 图层与前端交接

读取 Pencil schema 后按小批次构建和检查。中文语义命名；组件树接近未来前端树：Shell参考 / case1内容 / 左导航 / 右内容 / 描述 / KPI / 主视图 / 工具反馈。重复导航和 KPI 使用可复用组件或清晰一致的结构。

大区域使用明确的横/纵向布局、固定/fill/hug、gap/padding/overflow。五层菱形堆叠和连接线可局部固定坐标，但不要把全页每个元素绝对定位。所有 frame 固定 1920×1080，由共享 Shell 缩放，不做第二套响应式重排。注意隐藏/禁用/变暗的语义区别、长数字/缺值和局部提示不能裁切。

## 验证与最终交付

必须实际执行并保存：

- 四主帧全图截图；五层导航、KPI、模型工具与反馈的必要局部截图，放 `03-design/case1/_qa/`（本地证据，不自动提交）。
- `snapshot_layout` 和 `problemsOnly` 检查结果，以及主要组件的 `batch_get` 输出或可追溯摘要；放 `_qa/` 下 JSON/文本。包含真实 node/frame ID，不编造。
- `Visual_Diff.md`：逐帧、逐区域列明已修复/待修复/待用户接受的偏差。纯底图是已知待替换依赖，不因此声称整套已验收。
- `Frontend_Spec.md`：精确组件/图层、frame/node ID、布局规则、token、资产来源、数据绑定、点击区域、局部状态和三轨资源边界。不要发明 REST 字段或 GLB 相机坐标。
- `REVIEW-HANDOFF.md`：设计源路径、四 frame ID、证据路径、改动文件、重要差异、剩余风险，便于 Codex 回查。Codex 可能没有 Pencil MCP，因此导出的布局/节点证据和截图必须能独立读取，但不能把你自测说成独立验收。

完成后停止在可评审状态；不标记 APPROVED/frozen，不进入静态 HTML 或正式实现。若有阻塞，只报告真实阻塞和已交付成果，不重复询问已确认需求。
