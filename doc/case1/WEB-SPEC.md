# case1 正式 Web 施工规格

依据本轮明确授权，承接用户已通过的 Gate 1/1.5。契约见 API-CONTRACT.md。保留当前四帧布局、文案及素材；不重新设计。

- React 接入现有 Shell case1，Shell 独占缩放/导航；case1 卸载清理。静态 DOM 转为 JSX，资源复制到 code/web/assets/case1；正式代码不依赖设计、参考 UX 或 web-static 路径。
- 仅三层原生按钮，当前层再次点击回首页，暗层仍可切换；首页五图原色，详情非当前层 .35。业务 CSS 全部 case1-page 隔离。不迁移 DEMO、window 调试模拟入口、静态 Shell。
- 真实数值替换稿面占位，图轴/柱高/变化随数据变化；保持 KPI 卡/气泡视觉，遇到高值避免溢出。
- Three.js 分块加载，首页先画，再串行 fetch/parse 两个 GLB；切离中止网络，迟到解析结果释放。两层保留原材质，按需渲染/停止隐藏绘制。旋转、滚轮缩放、右键平移。本次 case1 内保留视角，切 case/刷新恢复 env 初值；无持久化、Reset、自动写配置。
- 初值及调试开关在 code/web/.env 的 VITE_CASE1_*；参数 debug 默认关闭，开启后显示当前相机/目标/模型参数、复制配置。修改 env 后重启开发或重新构建。
- RF 层进入后即将离线矩阵叠加到底图，使用 case2 的逐图 min/max、双线性与蓝青黄红透明网格；按底图原始像素校验 VITE_CASE1_HEATMAP_X0/Y0/X1/Y1，再与底图共同拉伸。支持视图缩放、旋转、平移、恢复配置初值及独立 RF 参数调试。不存在旧 VITE_CASE1_RF_OVERLAY 开关。
- 验证：静态三按钮/明暗/键盘/缩放；真实文件 API 数值及异常；React 生命周期与缓存；两 GLB 浏览器实际渲染和视角切换/切 case 清理；typecheck/build、受影响测试。记录本地参考文件证据，不能称真实后端验收。

## 2026-09-22 当前验收范围

用户确认当前 case1 前端开发完成并人工验证通过（含最近布局与 RF 增量）。应用默认进入 case1，导航名称为“DT构建”。3D 无 Reset；RF 的恢复按钮只恢复本次视图配置，不持久写入。代码冻结复核及独立验证边界见 QA-EVIDENCE.md。

开发冻结：2026-09-22，用户人工验收通过，授权修复后的代码与非浏览器验证完成；RF平移/鼠标缩放先转换屏幕坐标为Shell内布局坐标，旋转按可见宽度归一。冻结边界、证据及项目级既有构建限制见 QA-EVIDENCE.md。
