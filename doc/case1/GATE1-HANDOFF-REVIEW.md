# case1 Gate 1 交接复核

> 用户已裁决：不在静态页面补齐两项差异，以当前 Pencil 为标准。KPI 保留静态示意；Reset/保存反馈随持久化需求一并取消。下文为当时复核证据，原 P1 不再阻塞 Gate 1.5；不意味着旧交接文档中的已删除节点恢复存在。


2026-09-21。用户已明确人工视觉 PASS、结构检视通过；本轮 Codex 通过当前 Pencil MCP 直接读取设计源、实例展开结构、文字/透明度、布局问题并查看几何/RF 截图。不修改 `.pen`，不撤销用户视觉判断。

设计源：`03-design/case1/case1-dt-construction.pen`。四帧 `NATU1` / `J9nVp3` / `ag2DF` / `JsMgu`，均为 1920×1080。

## 结论

四主帧和左侧图片明暗符合已确认主线。但当前 `.pen` 已不同于 Cursor 交接文档：KPI 是示意数值，Reset/保存反馈及局部状态样板缺失。需明确这是用户有意保留、在网页阶段补齐的差异，还是需要回设计补齐，不能继续把旧交接文档当成当前设计事实。

## 具体发现

| 优先级 | 直接读取证据 | 实现影响与建议 |
|---|---|---|
| P1 | 四 KPI 实例 `Kpaun`、`tA05J`、`PZvse`、`HiBxF` 的 `lvH99=0.2`、`UrWuf=0.4`、`vwp31=50`；RF 轴仍为 0～1 | 与交接文档列出的 TXT 数值不符；0.2→0.4 也不是上升 50%。若保留为视觉示意，须显式注明仅取样式，数据和柱高按样例/契约另绑定；RF 正式 1.2 不能套 0～1 轴域。 |
| P1 | 全文档展开节点搜索 Reset/保存/复位/工具/状态样板/局部反馈返回空；几何 `B6COAr`、电磁 `zOjrh` 主视图区均只有代表底图 | 当前稿没有已要求的 Reset 和保存反馈；交接文档的 `d3LPp`、`ttrUa`、`sft2y` 不能作为可用节点指针。需确定在设计还是网页阶段补，避免实现漏功能。 |
| P2 | 文档旧导航图块 ID 与当前 `SjnFI`、`bgUfR`、`EZzsv`、`gyNQx`、`PtLoE` 不符；标签组件当前 `ky2W5` | 静态页从当前实例读取，不依靠旧 ID/近似尺寸。建议后续交接规格同步当前证据。 |

## 已核对正确

- 首页五图未降低透明度；三个详情只保留当前层原色，其他四图 opacity=0.35。
- 四帧采用共享 Shell、左侧导航和右侧内容层级；局部定位用于菱形组、视图区等。当前详情左侧宽 520、右侧宽 1320、内容 gap=30，并非交接文档近似的 500/gap20。
- KPI 标题、off/on 数字、柱形与徽标为独立节点，可以数据绑定；不是整页截图。
- 模型底图 context 明确运行时 GLB + Three.js；RF 底图 context 明确内嵌旧色块，未冒充 TXT 渲染。

## 布局提示的证据边界

当前 `Get` 的 `ctx.problems` 返回 home 4、geometry 11、material 8、rf 9 条提示，与旧报告“无问题”不同。包含底图主动超出 clip 容器、虚线/轴/徽标越过局部父级等；其中 RF 跨柱虚线 `HiBxF/EmFww` 被标 fully clipped。不能把所有提示直接当页面缺陷，也不能继续引用旧“清零”。用户已接受整体视觉，本轮不为装饰裁切重排；后续静态页按当前视图验证实际遮挡和可点击区域。

## 下一步

用户明确上述两项为网页阶段补齐时，记录接受边界、同步交接说明并继续 Gate 1.5；否则只回补必要设计节点和数据代表态，不重做已接受布局。当前尚未开始静态页面，以免擅自新增用户刚验收画面中不存在的控件。
