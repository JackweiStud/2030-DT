# case4 接口输入与待确认项

本文是资料核对与契约准备记录，不是已批准的 API 契约。Excel 的 CASE4 页第 18–29 行为后一组说明；前一组在第 2–13 行，不能混用冲突字段。

## 文件与 UI 映射

文件名均相对 `01-参考资料/case4/data/`；正式共享子目录尚未冻结。

| 文件 | 实际样本形状 | 展示用途 / 读取意图 |
|---|---|---|
| `ue_position_coordinates_base.txt` | 38×3，逗号 | 初始化基准轨迹 |
| `ue_position_gaode_coordinates_realtime.txt` | 38×3，逗号 | 商用方案实时轨迹 |
| `ue_position_without_dt_coordinates_realtime.txt` | 38×3，逗号 | 传统基站实时轨迹 |
| `ue_position_with_dt_coordinates_realtime.txt` | 38×3，逗号 | DT 辅助实时轨迹 |
| `ue_position_gaode_coordinates_realtime_cdf.txt` | 100×2，空白 | 完成后商用方案 CDF |
| `ue_position_without_dt_coordinates_realtime_cdf.txt` | 100×2，空白 | 完成后传统基站 CDF |
| `ue_position_with_dt_coordinates_realtime_cdf.txt` | 100×2，空白 | 完成后 DT 辅助 CDF |
| `ue_position_without_dt_thrp.txt` | 38×1 | 独立时间序列，传统基站吞吐率 |
| `ue_position_with_dt_thrp.txt` | 38×1 | 独立时间序列，DT 辅助吞吐率 |
| `ue_position_with_dt_coordinates_reflection_point.txt` | 20×8，空白 | 反射点数据；用户确认后续支持，首版不渲染路径 |
| `ue_position_with_dt_error_and_nlos.txt` | 4×2，空白 | 前三行为传统基站/商用/DT 的 CEP50、CEP90；第四行首列为平均 NLOS 比例 |

Excel 另列 `ue_position_map.png`，上述两次提交未提供该文件。用户已确认 case3/case4 同场地，直接复用当前 case3 地图及坐标映射；因此该缺失不再阻塞首版底图。地图和误差区点位推进示意也参考当前 case3。

## 已明确覆盖原说明的内容

- 用户明确吞吐率约每秒一个样本，与轨迹点数无关；覆盖 Excel “N 个点位对应、每个坐标点实时读取”的描述。样本恰好都是 38 行不构成绑定关系。
- 用户明确单次开始、完成后重置；不套用 case3 双侧顺序启动。
- 用户明确首版 2D、Pencil 由 Cursor 负责。

## 参考样本核对（2026-09-13）

以下为仓库参考输入的计算结果，不是真实测试结论，也不批准修改参考文件。

| 方案 | 汇总 CEP50 / CEP90（米） | 假设 base 为逐行真值时的 XY 平均距离（米） | 吞吐率样本均值（Gbps） |
|---|---|---|---|
| 商用 gaode | 3.340 / 8.051 | 0.088 | 无对应文件 |
| 传统基站 without_dt | 1.364 / 3.545 | 0.136 | 9.005 |
| DT 辅助 with_dt | 0.146 / 0.465 | 0.090 | 8.426 |

计算依据：CEP 直接取 error_and_nlos 文件前 3 行并按 Excel 语义映射；XY 距离为逐行 sqrt((x-baseX)^2+(y-baseY)^2) 后取均值；吞吐取各文件算术均值。有 DT 吞吐均值相对无 DT 约低 6.4%。

汇总统计支持 DT 定位精度优势，但当前 XY 轨迹误差未体现相同优势与尺度。不能认定轨迹、CDF、CEP 是同一轮同一口径的自洽数据；是否同源、是否采用不同统计窗口仍需澄清。用户认可定位精度为核心、同时展示吞吐及 NLOS 的主线；没有授权据此加工真实数据或替换参考样本。

## 打桩模式补充确认

用户要求同时支持原始参考数据 replay 和基于参考数据的 random 动态测试。已核对 `code/back/case2/case2-stub.mjs` 的 createPublisher 与 `code/back/case3/src/kpi-generator.mjs` 的 createRoundDataset：两者均有 replay/random 分支；case3 随机仅改吞吐/Cost，不能据此默认 case4 的随机字段或吞吐优劣。case3 的 fixture 还含自身 Cost 覆盖值，不能把其“回放 fixture”误解成 case4 也允许覆盖原始参考值。

## 已核对的收齐机制与 case4 决定

- case2 `code/server/src/cases/case2/data-files.mjs`：完成态六文件整批读取，校验读取前后文件快照和控制归属，任一失败不返回部分结果。
- case3 `code/server/src/cases/case3/side-files.mjs`：多文件取连续完整行的共同前缀，未齐部分标 pending，适合实时点位。
- 用户已确认 case4 三方案同一行收齐后，再一起显示该点轨迹与误差。只借鉴收齐机制，不继承 case3 吞吐与点位绑定。用户已确认 case4 完成统计的整批门槛：三份 CDF 与一份 CEP/NLOS 汇总文件全部读取、校验成功，才一起展示最终统计并启用重置；最终结果读取失败恢复已获用户确认：参考 case3 控制器 noteFinalNotReady，累计失败 10 次（含首次）后退出测试并提示结果不完整、尝试 init，适配正常时允许重新开始，init 失败标适配异常。具体控制字段和轮询周期另行定稿。

## 后端四项问题澄清记录

已单独整理为 [case4 后端待确认问题](BACKEND-QUESTIONS.md)，包含 dt_type、65535、最终轨迹点数门槛、两条吞吐率序列对齐四项，并提供答复位置。2026-09-15 四项已完成核心澄清，结论统一记录在该清单，后续据此编写契约，不再按未答复处理。

## CEP 百分比补充确认

用户于 2026-09-15 确认：CEP50/90 分别展示 DT 辅助相对传统基站定位的变化，预期降低。以对应分位的传统基站值为分母，降幅 = (传统基站 CEP - DT CEP) / 传统基站 CEP × 100%。计算使用读取后的数值，不使用标签舍入值；正值显示降低，负值显示增加及其绝对百分比，零显示无变化；基准为 0 时显示“—”，不除零或编造比例。商用方案仍展示 CEP 数值，不作为该百分比基准。预期降低不代表强制降低，不使用固定百分比。

用户已明确截图只作示意，不改变接口行语义。当前样本为传统 CEP90=3.5452253591553533、商用=8.051417720457543、DT=0.465079644212001；展示按接口数据映射，不照抄截图数值，本项疑问已关闭。

## 待确认清单

| 项目 | 证据 / 差异 | 契约需明确 |
|---|---|---|
| 随机关联与幅度 | 用户确认吞吐率、定位轨迹小幅变化，误差统计与 NLOS 每轮变化；CDF/CEP 沿用后端参考统计小幅扰动，不从随机轨迹重算 | 用户已明确最终定位统计不强制三方案优劣排序；用户明确只需简单小幅变化，不新增专门的幅度配置项，具体保守幅度由实现处理；CDF 单调性、概率范围等数据有效性仍应满足；基准轨迹及反射点未获随机化授权；须标记轨迹与统计为独立模拟数据 |
| 逐点偏差与配对 | 用户明确 base 为预期轨迹，realtime 为定位结果；2D 欧氏距离计 XY，3D 计 XYZ；不把预期轨迹认定为实测真值 | 用户确认按文件行号与 base 对应，第 i 行为 Pi；65535 已由用户澄清为同方案按分量用上个点对应值替代并记日志，不跳点；首点无前值时已确认使用 base P1 对应分量；连续无效沿用前一点已归一值；逐点偏差不替代后端 CDF/CEP |
| 反射点格式 | 旧说明为 x,y,z,losFlag；新说明为 losFlag,n,多组 XYZ，但维度仍写 N×4；样本 n=2、8 列 | 首版不渲染直射/反射路径，不将该文件纳入三方案轨迹收齐门槛；可变列格式及 20 行如何对应 38 轨迹点留待后续反射功能处理 |
| 实时发布 | 用户已确认实时文件 append | 已确认三方案同一行收齐后同步展示，按连续完整点位推进，吞吐率独立；用户已确认轨迹及吞吐文件均 append 新行；用户已确认开始/重置前由 Node 清空 case4 本轮动态文件，保留预期轨迹、地图及原始参考样本；半行处理、具体清理清单仍待明确；用户已明确确认最终轨迹沿用 case3：不要求等于 base 点数，但三方案须相互收齐并通过完整性门槛，不补造缺点 |
| 吞吐率横轴 | 用户明确显示“1、2、3…样点”，按各吞吐文件的采样序号排列，不换算秒数、不绑定地图 Pi；约每秒一个样本 | 后端已明确两条曲线各自有数据就更新，不互相收齐；重复轮询不得重复造点；不要求文件新增时间戳 |
| 控制与终态 | 未有 case4 冻结控制契约 | start 的 dt_type 已确认使用精确字符串 `"with dt"`，reinit 同为 `"with dt"`，init 为 `""`，已确认；case4 已确认沿用 case2/case3 的 init/start/reinit 与 execute success/execute fail/case complete/reinit complete 状态语义；文件关闭顺序仍需写入契约，最终轨迹按 case3 原则不要求等于 base 点数（最终统计四文件门槛已确认） |
| 结果统计 | 后端提供 CDF 及独立 CEP/NLOS 汇总 | 以哪份为权威、缺文件/非法数值如何处理；不能从示意图硬编码或强行重算 |
| 数值解析 | 坐标逗号、CDF/反射点/汇总空白；样本存在高精度及科学计数；新版概率精度 0.001 | 可接受分隔符、科学计数、坐标/误差/概率精度、范围与无效值规则 |
| 恢复与交互 | 正常三态已清楚 | 最终结果读取失败已确认有限 10 次后回退并允许重新开始（适配正常时）；刷新已确认沿用 case3：撤销旧轮、清空前端展示、初始化成功后重新开始；Start/ReInit 失败已确认仅允许手动重试对应命令、不自动重发；用户已确认首版三方案同时显示、不提供单方案隐藏开关；误差浮层已确认仅完成后悬停误差回溯区 Pi 显示三方案逐点误差；切回规则仍待明确 |
| 截图 | 用户已确认沿用 case3：后端请求、Web 生成、Node 保存并清标志；输出隔离于 out/case4/ | 按当前 case3 实现核对并写入 case4 契约的窗口、重试与结果收尾细节；不重复询问是否需要 |

上述问题按用户最新要求每轮只问一个，并根据回答决定后续追问；关键需求充分明确前，不给出具体行动计划和施工细节。不能把尚未确认的协议反向要求后端团队执行。
