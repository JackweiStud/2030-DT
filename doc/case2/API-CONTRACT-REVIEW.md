# case2 Gate 2 API 契约评审

## 摘要

- 评审对象：[API-CONTRACT.md](API-CONTRACT.md) `v0.3`。
- 评审范围：case2 文件控制、结果发布、UI 数据来源、截图语义；不评审 Node/React 实现。
- 结论：`DRAFT / 不批准进入 Gate 3`。主线语义已可评审，但四项外部事实尚无证据，不能用文档猜测补齐。

## 修

| 问题 | 为什么会影响演示 | 必须修改/已固定的处理 |
|---|---|---|
| “清除 / 重置”双口径 | 前端、后端与演示人员会把同一操作理解为不同命令 | 已固定：UI 文案为“重置”，唯一协议映射为 `command=reinit`。 |
| `execute success` 被误作完成 | 会提前读取旧文件或半成品，或把重置命令成功误作重置完成 | 已固定：`execute success` 只表示命令执行成功；启动路径继续等 `case complete`，重置路径继续等 `reinit complete`。 |
| 参考样本形状被误写为正式限制 | 后续真实后端可能输出非 20×20 热力矩阵或非 20 条 KPI 样本，前端会错误拒绝合法数据 | 已固定：热力图为动态 `Nx × Ny` 非空矩形矩阵，KPI 为动态 `N` 个有限样本；当前 20×20/20×1 仅是参考样本证据。 |
| `reinit` 成功口径不清 | 前端可能一直等待后端回 `command=init,status=""`，导致按钮和数据移除状态卡住 | 已固定：`status="reinit complete"` 即重置完成；前端据此移除 Calibrated 数据并恢复登录时按钮状态。 |
| `execute fail` 后续终态不清 | 前端可能继续等待 `case complete` / `reinit complete`，导致界面卡在等待态 | 已固定：`execute fail` 是命令失败终态；前端显示执行命令失败，后端本轮不再写完成终态。 |
| 静态原型中的数字/代表热图易被沿用 | 会把 40%、50% 或代表层当成运行时结果 | 已固定：CDF、均值、降幅由当前 KPI 样本派生；正式热图运行时绘制。 |

## 跳

| 项目 | 为什么跳过 | 风险/边界 |
|---|---|---|
| REST 路由、端口、JSON response shape | 当前没有已确认的本机适配服务实现或部署事实 | Gate 3 必须在不改变本文语义的前提下补齐；本阶段不得虚构 `/api/...`。 |
| 轮询周期、超时秒数、重连退避 | 当前没有真实共享目录或运行测量 | Gate 3/5 以真实环境确定；Web 仍必须区分服务故障与业务失败。 |
| 文件锁、原子写代码、共享目录路径 | 属于实现/部署决策，尚未冻结 | 不进入 Gate 2 文档或代码；结果必须满足完整同批的语义要求。 |
| AOA/ZOA 与 `without dt` | 不驱动当前可见 UI 或演示主线 | 不得作为“未来兼容”字段混入 case2 正式结果批次。 |

## 待确认（阻塞 v1 批准）

| ID | 待确认事项 | 所需确认者 | 最小可接受证据 |
|---|---|---|---|
| P0-1 | 后端如何证明六个 Calibrated 文件同批、完整且属于本次运行 | 后端负责人 + 用户 | 一页发布顺序/批次标记规则，覆盖失败和重试。 |
| P0-2 | 命令超时、重复点击、刷新中断与失败后的按钮解除/重试策略 | 前端/部署负责人 + 用户 | 一条异常交互时序，说明按钮何时解除、是否可重试、如何提示。 |
| P0-3 | 适配服务的逻辑操作如何落到本机 REST、挂载路径、锁和超时 | 前端/部署负责人 + 用户 | 路由表与责任边界；不需要实现代码。 |
| P0-4 | 截图输出、成功回执、失败重试和重启去重 | 前端/部署负责人 + 用户 | 一条成功与一条失败时序，证明未落盘不会清零。 |

## Gate 2 通过条件

- [x] 正常、失败、重置、刷新/切 Tab 与截图的语义已写入契约。
- [x] 所有可见动态区块已在 `doc/case2/UI-DATA-SOURCE-MAP.md` 反向映射。
- [x] 数据真假边界、三项指标范围和前端派生公式已写明。
- [ ] P0-1 至 P0-4 已确认并回填契约。
- [ ] 用户批准 API 契约 v1。

在最后两项完成前，Gate 2 保持进行中；不得进入 Gate 3 或任何真实读写实现。
