# Pencil Plan — case4 DT for positioning

## 状态

**计划已执行完毕。** 定稿件为 `03-design/case4/case4.pen`（2026-09-15 Gate 1 冻结）。下文保留为「曾如何重建」的档案；**当前结构以 `.pen` + `Freeze_Note.md` + `Frontend_Spec.md` 为准。**

## Target（已达成）

- Output：`/Users/jackwl/Code/2030-DT/03-design/case4/case4.pen`
- Canvas：1920×1080；业务态同构树 + 显隐/进度区分
- case3V1 整页：**已从本文件移除**（不再作为交付或基线帧留在画布）

## 定稿帧（替换原计划中的 case4.* 列表）

| Frame | ID | 说明 |
|---|---|---|
| Case4-空闲态（同构·仅显隐与进度） | `mEyG6` | 原「初始」 |
| Case4-测试中（同构·仅显隐与进度） | `YFD1U` | 运行代表态 |
| Case4-完成态（同构·仅显隐与进度） | `iVPf8` | 最终展示 |
| case4.现场环境 | `PyHHC` | 同构底稿 + 现场环境浮层 |

**计划中但未保留为独立整页的态**（实现见 Frontend_Spec）：完成悬停、等待最终统计、异常状态板。

## 定稿组件树

```text
Case4Frame
├── 共享顶栏（Shell叠层最上）          ref HeJpZ
├── 地图舞台（裁切视口）
│   └── 地图变换层（唯一坐标原点·含校准偏移）
│       ├── 预期路径走廊（预置·含校准偏移）
│       ├── 动态位置（虚拟进度·非实测）
│       ├── 路线点位组（预置·含校准偏移）
│       ├── 轨迹侧标
│       └── 实际轨迹（实测·含校准偏移）
├── 地图浮层挂载（absolute·非文档流）
│   ├── 顶部蒙版-浮层
│   ├── 测试对比入口（按钮·横排flex）
│   ├── 现场环境入口（文本热区·非按钮框）
│   ├── 视图切换
│   └── 轨迹图例
└── 底部数据浮层（y640叠层）           vertical
    ├── 定位误差回溯区（横排·flex）
    │   ├── 回溯控制列 / 误差Y轴
    │   └── 回放列区（列头/空槽/点叠层）
    │       ├── 点位列表头（列头行+进度浮层挂载）
    │       └── 误差点区（列窗几何+点叠层）
    └── 指标卡行（横排·flex）
        ├── 定位误差卡 / NLOS卡
        └── 吞吐卡占位（宽623·内嵌组件）→ 吞吐率对比卡
```

## 可复用组件（定稿）

| ID | 名称 | Notes |
|---|---|---|
| `HeJpZ` | 共享顶栏 | 实例改 Tab |
| `B6761` | 视图切换 | 3D 灰禁 |
| `c0EO9c` | 路线点位 | ×N 代表 20 |
| `QCplU` | 列头 | ×20 |
| `X607U` | 回溯控制列 | 单次开始/重置 |
| `wYizf` | 误差Y轴 | |
| `Fcn8x` | 定位误差卡 | CDF+CEP |
| `P6EMJ` | NLOS卡 | |
| `fYdoo` | 吞吐率对比卡 | X=样点 |
| `bfpB8` | 轨迹图例 | 不可点显隐 |

已删除：未使用 `图例项`、游离 `轴体`。

## Layer / Layout 合同（交接后写入层名）

- Pencil：`layout` 缺省 = 横排 flex；`vertical` = 竖排；`none` = absolute。
- 误差：`列几何槽-P*（空·仅定宽）` + `误差点叠层（对齐列中心·非抄left）` + `点列（按P索引绑定·非抄left）`。
- 连线：`连线（示意·非实现源）`。
- 地图子层带「含校准偏移」→ 同一变换矩阵。

## Tokens（Pencil 已存在）

沿用 + case4：`c4-plan`、`c4-bs`、`c4-gaode`、`c4-dt`、`c4-dock`、`c4-page`、`c4-wait` 等。以文件内变量为准。

## 示意数据（非契约）

| 项 | 空闲 | 测试中 | 完成 |
|---|---|---|---|
| 窗口 | P1–P20 空叠层 | 约 P1–P13 | P1–P20 |
| CDF/CEP/NLOS | 空/等待 | 等待 | 有值示意 |
| 吞吐 | 空 | 双线 | 保留 |

## 归档说明

本文件不再指导「下一步怎么画 Pencil」。改视觉须解冻后改 `.pen`，并同步 `Freeze_Note.md` / `Frontend_Spec.md` / `Visual_Diff.md`。
