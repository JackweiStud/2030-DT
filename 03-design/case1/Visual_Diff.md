# case1 Visual Diff

状态：`REVIEW_READY`（非 APPROVED / 未冻结）  
设计源：`03-design/case1/case1-dt-construction.pen`  
对照源：`01-参考资料/case1/ux/` 四主图  
证据：以 Pencil 打开设计源目视 + `Frontend_Spec.md` node id；过程目录 `_qa/` 已删除不入库

## 工具检查摘要

| Frame | Node ID | problemsOnly（设计完成时） |
|---|---|---|
| case1-home | `NATU1` | 无 |
| case1-geometry | `J9nVp3` | 无 |
| case1-material | `ag2DF` | 无 |
| case1-rf | `JsMgu` | 无 |
| 局部状态样板区 | `sft2y` | 无 |

此前出现过首页第三张缩略图轻微裁切与 RF 色标轴标裁切，已收紧 gap/尺寸后复检清零。

## 逐帧差异

### case1-home

| 区域 | 状态 | 说明 |
|---|---|---|
| 顶栏 | 有意差异 | 采用当前运行 Shell（DT Construction / IMT2030(6G)无线数字孪生），不用 UX 旧顶栏 |
| 左五层导航 | 已重调（2026-09-21） | 对齐 UX：标题带图标；1–4 层同卡重叠菱形堆叠；应用层独立底卡；标签左对齐图块中线；首页全亮。图块 ID 见 `Frontend_Spec.md` |
| 右内容面板底 | 已套用资产 | 四主帧右侧内容使用 `bg-right.png` |
| Layer 3 文案 | 已修正 | 设计为「RF 层」，不再用 UX 重复「电磁层」 |
| 右五层说明行 | 已对齐结构 | 每行横向 flex：描述/标签 + 三配图；使用 `assets/home/` |
| 配图比例 | important | 行高受 1000px 内容区约束，缩略图高度低于 UX，优先保五行走齐 |

### case1-geometry

| 区域 | 状态 | 说明 |
|---|---|---|
| 左导航暗态 | 已实现 | 几何原色，其余 opacity 0.35 |
| KPI | 已替换样例 | 0.85/0.90 ↑5.9%；0.82/0.91 ↑11.0%；轴域按 0～1 柱高 |
| 主视图 | 代表图 | `geometry-3d.png`；注释标明正式 GLB+Three.js |
| 工具/反馈 | 新增 | Reset +「视角已保存」局部反馈，不遮挡导航 |

### case1-material

| 区域 | 状态 | 说明 |
|---|---|---|
| KPI | 已替换样例 | 电磁信道保真度 0.88/0.92 ↑4.5% |
| 材质图例 | 已实现 | 玻璃/混凝土/木材/金属/其他；标注仅说明、不筛选 |
| 主视图 | 代表图 | `material/地图.png`；反馈示例为「保存中…」 |

### case1-rf

| 区域 | 状态 | 说明 |
|---|---|---|
| KPI | 已替换样例 | RSS 误差 1.20/0.80 ↓33.3%；柱高按上界适配，不套 0～1 |
| 热力色标 | 已按 case2 方向 | 蓝→青→黄→红；不用旧红橙黄绿条 |
| 地图 | 已知待替换 | `rf-map.png` 内嵌旧色块；**不是** TXT 渲染结果；纯底图到位前不验收最终热力颜色/投影 |
| 3D 工具 | 不适用 | RF 无 Reset/3D 工具区（与范围一致） |

## 资产与结构

- 未把整张 UX PNG 当页面背景。
- 可复用组件：Shell、左导航、KPI 卡、标签、工具区、反馈条。
- 大布局以横向/纵向 flex + fixed/fill/hug；仅五层堆叠与视图浮层使用局部 absolute。
- 已清理未引用重复拷贝；`bg-left-*` / 多余 swatch / 备用 icon 暂留，由用户自行处理。

## 待用户接受 / 待依赖

1. 五层菱形堆叠的精确透视间距与 UX 是否需再贴像素（当前结构优先）。
2. RF 纯底图替换后的锚区与热力最终色。
3. 材质图例是否要用源图裁切替换色块示意。
4. 最终视觉冻结（仅用户可标 APPROVED）。
